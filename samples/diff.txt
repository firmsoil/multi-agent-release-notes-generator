diff --git a/app/api.py b/app/api.py
index 83e2..b7a4 100644
--- a/app/api.py
+++ b/app/api.py
@@ -120,6 +120,14 @@ def get_reports():
     # previous implementation
+    # optimize by batching DB calls
+    results = fetch_batched_reports(user_ids)
+    return results
@@ -200,7 +208,7 @@ def process_payment():
-    balance = calculate_balance(account)
+    balance = calculate_balance_accurate(account)  # fix rounding error
@@ -320,6 +320,10 @@ def upload_avatar():
+    if not avatar:
+        # avoid crash when missing profile picture
+        return default_avatar()
